# Copy to .env in this same directory, fill in real values, never commit it
# (.env is gitignored). Load with: set -a && source .env && set +a
# then run the binary/`go run` in the same shell.

FLICKRGO_API_KEY=
FLICKRGO_API_SECRET=
FLICKR_OAUTH_TOKEN=
FLICKR_OAUTH_TOKEN_SECRET=

R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=flickr-motion-photos-videos
R2_PUBLIC_BASE_URL=
